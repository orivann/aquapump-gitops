---
ingress:
  frontend:
    hosts:
      - host: stage.aqua-pump.net     
  backend:
    host: api-stage.aqua-pump.net

backend:
  existingSecret: aquapump-secrets

frontend:
  existingSecret: aquapump-secrets

